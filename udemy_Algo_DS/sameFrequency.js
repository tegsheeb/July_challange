

function sameFrequency(num1, num2){
  // good luck. Add any arguments you deem necessary.
  const lookup = {};

  while(num1 > 9) {
      let digit = num1 % 10;
      lookup[digit] ? lookup[digit]++ : lookup
  }

  return false;

  return true;
}
